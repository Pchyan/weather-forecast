name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # 或您想要的分支

jobs:
  build:
    runs-on: ubuntu-latest

    #
    steps:
    #  - name: Checkout code
    #    uses: actions/checkout@v2

    #  - name: Set up Node.js
    #    uses: actions/setup-node@v2
    #    with:
    #      node-version: '14'  # 或您需要的 Node.js 版本

    #  - name: Install dependencies
    #    run: npm install  # 如果您使用 npm 來管理依賴

    #  - name: Build project
    #    run: npm run build  # 如果您有構建步驟

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist  # 您的構建輸出目錄
          publish_branch: gh-pages  # 您想要部署的分支
          api_key: ${{ secrets.API_KEY }}  # 使用 GitHub Secrets 中的 API 密鑰
